# 1.16.1

伙伴插件正式版。已验证宿主：**DSH 0.1.5-rc.1**；要求 Node.js **^22.19.0 或 >=24.0.0**。保留旧宿主兼容分支，未重新完成全部旧宿主端到端验证。

- Matrix 发送 `org.matrix.custom.html` 富文本及文本兜底，支持标题、强调、引用、列表、代码块和表格。
- Mattermost 使用原生 Markdown；普通回复与主动通知共用格式适配。
- 按 UTF-8 字节预算和 Markdown 块分段；长代码补齐围栏，长表格重复表头，引用链接独立化，不截断 Unicode 字符。
- 原始 HTML 转为文字，危险 URL 不生成链接，远程图片转为链接。极端超长单个单元按字面代码分段，优先保全内容。
- Markdown 依赖打包至服务端模块，不更改宿主核心依赖版本。

385 项自动化测试通过；已部署，线上模块加载、API 和页面检查通过，用户已确认效果。旧消息不会自动更新；具体富文本显示依赖接收客户端，不新增 E2EE 或附件支持。
