# 内置影视创作技能

内置市场提供两个独立入口，需手动安装并在伙伴能力中授权，不自动启用。

| ID | 名称 | 用途 | 上游固定提交 |
| --- | --- | --- | --- |
| director-skills | 影视创作工作流 | 九模块的故事、剧本、分镜、视觉一致性、图像/视频提示词、模型适配与修订 | 0xhughs/director-skills @ 35ca0a2b4cd55b0668aa9ea0f40273324b774bb4 |
| cinematic-director | 导演与分镜设计 | 导演调度、关键帧、声音、剪辑与质量诊断，二十种导演方法参考 | wuwangzhang1216/DirectorSKILL @ c65ae0d14457053efb1e354c7e7f7e120d97fad1 |

## 加载与权限

中文 inline 入口保持精简，匹配任务后先读各自 `upstream/SKILL.md`，再按需读取所需参考和模板。不会将全部资料注入会话，也不会把九个子模块自动注册为工具或伙伴。两者同时启用时，前者负责阶段路由，后者仅细化相关导演环节。

资料需要已授权的文件读取工具；生成图像、视频、剪辑或文件交付需要对应工具或 MCP。技能不授予工具权限，不承诺资料中的模型能力仍然有效。多伙伴制作沿用需求/任务看板，外部长任务沿用一次性续接，不绕过授权、审批或预算。

## 分发与兼容

`resources/skills/<id>/upstream` 包含上游 Markdown 资料和 MIT LICENSE；仅统一为 LF 换行及末尾换行，不改写原文。CI 配置、运行脚本和非文档评测数据不随插件分发。

每个包的 `manifest.json` 保存来源、提交和逐文件 SHA-256；安装时校验并使用现有原子包安装器。安装无需 GitHub 连接；升级只跟随已审查的插件版本，不实时追踪上游。初始化检查已安装内置包的资源，缺失或过期时重装并保留伙伴绑定，不自动安装未选中的技能。

DirectorSKILL 上游使用多行描述和嵌套 YAML 元数据；本次不扩大通用解析器职责，使用兼容的薄入口，完整原文作为参考文件读取。入口版本与上游版本分别记录。

上游许可证分别位于两个资源包的 `upstream/LICENSE`，会一同写入已安装技能目录。影片、人物或素材权利不由这些 MIT 文档许可证授予。
